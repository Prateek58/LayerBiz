query

I have a simple static blog website built in nextjs. feature: special for light wieight landing page website cms and blogs. must have : seo optimized , (like most imp feature like rankmath top 20% feature so user dont have to learn this complex ) is out of the box without complicated rules and learning curve. and min basic features like gallery, categorization (tags, category, post, project gallery, compare sheet page (pricing) , team page, contact us page, out of the box caching /image compression/ or handling (webp etc) , mail logs(like user can see succes/fail mails , integrate with comon provider(initally with google mail api), spam protection / captcha, login using mobile otp, and all the most useful things, easy backups (gdrive for now) , easy payment gateway (for future not now) , .. all for which user have to waste their time to check wordpres plugins and to buy plugins..  i was planning to make strapi backend, so i can put dynamic content from backend, 

